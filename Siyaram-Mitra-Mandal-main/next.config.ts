import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  devIndicators: false,
  reactCompiler: true,
  allowedDevOrigins: ['10.29.158.47'],
};

export default nextConfig;
